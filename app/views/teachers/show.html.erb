<h1>講師詳細資訊</h1>

<table class="table">
    <thead>
        <tr>
            <td>講師</td>
            <td>描述</td>
            <td>處理</td>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= @teacher.name %></td>
            <td><%= @teacher.description %></td>
            <td>
                <%= link_to "編輯", edit_teacher_path(@teacher), class: "btn btn-default" %>
                <%= link_to "刪除", teacher_path(@teacher), method: "delete", data: { confirm: "確認刪除此講師？" }, class: "btn btn-danger" %>
            </td>
        </tr>
    </tbody>
</table>

<%= link_to "回講師列表", teachers_path, class: "btn btn-default" %>